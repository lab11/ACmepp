DEFINES+=PROJECT_CONF_H=\"project-conf.h\"
CONTIKI_PROJECT = failsafe

TARGETDIRS += ../../platform

UIP_CONF_IPV6=1

all: $(CONTIKI_PROJECT)

CONTIKI = ../../../../contiki
include $(CONTIKI)/Makefile.include
